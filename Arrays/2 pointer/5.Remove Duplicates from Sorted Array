nums = [1,1,2,2,3,4,4]

left = 0

for right in range(1, len(nums)):
    if nums[right] != nums[left]:
        left += 1
        nums[left] = nums[right]

print(nums[:left + 1])<----------------------
