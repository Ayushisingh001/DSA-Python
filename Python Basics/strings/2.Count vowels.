2.Count vowels.


s="ayushi"
count=0
for ch in str:
    if ch=='a' or ch=='e' or ch=='i' or ch=='o' or ch=='u':
        count+=1
print(count)
Time Complexity: O(n)
Space Complexity: O(1)


more optimized approach-->
s="ayushi"
count=0
for ch in str.lower():
    if ch in 'aeiou':
        count+=1
print(count)
Time Complexity: O(n)
Space Complexity: O(1)
