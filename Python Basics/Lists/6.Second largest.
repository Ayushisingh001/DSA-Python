6.Second largest.

l=[1,3,4,6,8]
max = float('-inf')
second_max = float('-inf')
for num in l:
    if num>max :
        second_max=max
        max=num
    elif num>second_max and num!=max:
        second_max=num
print(second_max)

TC:O(n)
sc:O(1)
